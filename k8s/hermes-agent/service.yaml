apiVersion: v1
kind: Service
metadata:
  name: hermes-service
  namespace: hermes
spec:
  selector:
    app: hermes-agent
  ports:
    - protocol: TCP
      port: 8642
      targetPort: 8642
  type: ClusterIP # Use NodePort ou LoadBalancer se quiser acesso externo direto
