apiVersion: v1
kind: Service
metadata:
  name: heimdall-service
spec:
  type: NodePort
  selector:
    app: heimdall
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 31100
